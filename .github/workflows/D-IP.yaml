name: D-IP-name
on:
  workflow_dispatch:
  schedule:
    - cron: "15 15 * * *"
jobs:
  D-IP-jobs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@main
      - name: Run scripts
        run: |
          bash dev/D-IP.sh
          bash dev/git-push.sh
